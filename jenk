pipeline {
    agent any

    environment {
        // Ρύθμιση του Docker host, απαραίτητη σε περιβάλλον Windows με Docker
        DOCKER_HOST = 'tcp://localhost:2375'
    }

    stages {
        stage('Checkout') {
            steps {
                // Παίρνουμε τον κώδικα από το Git repository
                git 'https://github.com/jimtz/weather.git'
            }
        }

        stage('Static Analysis') {
            steps {
                script {
                    // Εκτέλεση SonarQube scanner μέσω Docker
                    sh 'docker run --rm -e SONAR_HOST_URL=http://your-sonar-server:9000 -v ${WORKSPACE}:/usr/src sonarsource/sonar-scanner-cli'
                }
                script {
                    // Εκτέλεση truffleHog για ανίχνευση ευαίσθητων δεδομένων στο repository
                    sh 'docker run --rm -v ${WORKSPACE}:/data trufflesecurity/trufflehog:latest trufflehog file:///data'
                }
            }
        }

        stage('Dynamic Analysis') {
            steps {
                script {
                    // Εκτέλεση Nmap scan, αντικαταστήστε `your-target-ip` με την πραγματική IP
                    sh 'docker run --rm instrumentisto/nmap -v -A your-target-ip'
                }
                script {
                    // Εκτέλεση SQLMap, αντικαταστήστε `target-url` με την URL που θέλετε να ελέγξετε
                    sh 'docker run --rm paoloo/sqlmap sqlmap -u target-url --batch'
                }
            }
        }

        stage('Docker Security Check') {
            steps {
                script {
                    // Έλεγχος εικόνας Docker με Trivy, αντικαταστήστε `your-docker-image` με την εικόνα προς έλεγχο
                    sh 'docker run --rm aquasec/trivy:latest image your-docker-image'
                }
            }
        }

        stage('Deploy') {
            steps {
                script {
                    // Προσθέστε τα βήματα για το deployment της εφαρμογής σας
                    echo 'Deploying Application...'
                }
            }
        }
    }

    post {
        always {
            echo 'This will always run'
        }
        success {
            echo 'This will run only if successful'
        }
        failure {
            echo 'This will run only if failed'
        }
        unstable {
            echo 'This will run only if the run was marked as unstable'
        }
        changed {
            echo 'This will run only if the state of the Pipeline has changed'
        }
    }
}
